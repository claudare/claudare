name: hello_world
description: A minimal hello world package for the Claudare workspace.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
