name: id_generator
description: 128-bit ID generator contracts and implementations.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.7.2

dependencies: {}

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
