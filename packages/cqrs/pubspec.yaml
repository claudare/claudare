name: cqrs
description: CQRS infrastructure for the Claudare workspace.
version: 1.0.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.7.2

dependencies:
  claudare_logging: ^1.0.0
  common: ^1.0.0
  id_generator: ^1.0.0
  time_provider: ^1.0.0
  queue: ^1.0.0
  mutex: ^3.1.0
  isolate_sqlite: ^0.3.1
dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.1.0
  test: ^1.24.0
