name: common
description: Shared async coordination, causal, serialization, and pagination primitives.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
